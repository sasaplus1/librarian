{
  "devDependencies": {
    "pug": "^3.0.1",
    "pug-cli": "^1.0.0-alpha6"
  },
  "private": true,
  "scripts": {
    "build": "pug --pretty --path . --basedir .",
    "fixpack": "npx fixpack"
  }
}
